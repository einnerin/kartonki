ПРОДОЛЖЕНИЕ ПОЛИРОВКИ (возобновление после паузы)
Прочитай docs/claude/polishing_plan.md раздел со статусом — там точка остановки предыдущей сессии.
ОГРАНИЧЕНИЯ ЭТОЙ СЕССИИ:
- Максимум 3 чанка (15 setId) — потом обязательно stop с отчётом и обновлением plan
- Не уходить в extended thinking режим надолго — если один чанк идёт >30 минут, остановись и зафиксируй проблему
ОЧЕРЕДЬ (продолжай с точки остановки):
1. WordDataEnglish.kt — оставшиеся чанки до 89
2. WordDataEnglishExpanded.kt — text-author по чанкам
3. Иврит в БЕЗОПАСНОМ РЕЖИМЕ (см. polishing_plan.md — только definitionNative, exampleNative, metadata; НЕ трогать ивритский текст)
АЛГОРИТМ (тот же что в прошлых сессиях):
definition-polisher → example-polisher → metadata-filler → validate → build → bump WordDataVersion → commit → push
Чанки по 5 setId. Один коммит на чанк.
ПРАВИЛА АВТОНОМИИ:
- Не спрашивать подтверждений между коммитами
- Не использовать git add -A или --no-verify
- maven_proxy.js для сборок: node maven_proxy.js > /tmp/mp.log 2>&1 &
- Pre-commit hook в targeted-режиме
СТОП-УСЛОВИЯ:
1. Достигнут лимит 3 чанка — финальный отчёт + stop
2. Hook блокирует, починить не удаётся за 3 попытки
3. Валидация падает после двух полных проходов на одном setId
4. Сборка падает
5. Структурная аномалия в файле
6. На иврите: validate падает на поле definition/example (ивритский текст) — это ожидаемо, зафиксировать в plan, коммитить остальные правки как есть
ФИНАЛЬНЫЙ ОТЧЁТ в конце сессии:
- Сколько чанков сделано
- Сколько setId закрыто
- Точка остановки для следующей сессии (явно в polishing_plan.md)
- git log --oneline последних N коммитов
- git status должен быть clean
Перед стартом: прочитай polishing_plan.md, убедись что рабочая копия чистая (git status), что maven_proxy запускается.
ПОЕХАЛИ.
