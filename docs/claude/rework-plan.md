# Рефакторинг наборов слов — план и прогресс

> **ЧИТАЙ ЭТОТ ФАЙЛ ПЕРВЫМ** перед любой работой с наборами слов в рамках рефакторинга.
> После каждого изменения обновляй статус набора прямо здесь.

---

## Цель

Привести **все** наборы к единому стандарту:

| Критерий | Требование |
|----------|-----------|
| Размер | Ровно **25 слов** |
| Тема | Все слова однозначно относятся к **одной теме** |
| Уровень | Все слова **близкой сложности** (например, A1–A2 или B1–B2) |
| Синонимы | Запрещены — два слова с одним смыслом в одном наборе бессмысленны |
| Однокоренные | Запрещены — `run`/`runner`, `day`/`daily` не несут обучающей ценности |

---

## Трёхуровневая структура (для богатых тем)

Если тема охватывает слова от A1 до C2 — она **разбивается на три набора**:

```
Тема: основы      → COMMON/UNCOMMON  (A1–A2)   — 25 слов
Тема: продвинутый → RARE             (B1–B2)   — 25 слов
Тема: углублённо  → EPIC/LEGENDARY   (C1–C2)   — 25 слов
```

Каждый из трёх — самостоятельный набор, ровно 25 слов.

---

## Правила рефакторинга (ОБЯЗАТЕЛЬНО)

1. **Читай набор целиком** прежде чем что-то менять.
2. **Убирай слова не своего уровня** — если в "основах" есть EPIC-слово, оно туда не подходит.
3. **Заменяй, а не переименовывай** — нельзя менять `rarity = "EPIC"` → `"RARE"` на том же слове. Сложность слова объективна, метка её не меняет. Нужно заменить слово на другое, реально подходящее по уровню.
4. **Убранные слова не теряй** — они уходят в набор своего уровня (продвинутый/углублённо), существующий или новый.
5. **Набор после правки = ровно 25 слов.** Проверяй: `grep -c "setId = N," файл.kt`

---

## Известные проблемы (исправить в первую очередь)

### Set 211 — "Архитектура" с 281 словом
Set 211 называется "Архитектура" но содержит **281 RARE-слово** из 9 разных тем
(Супермаркет, Инструменты, Праздники, Профессии, Быт, Музыка, Кино, Кулинария, Садоводство, Интернет).
Это "мусорная корзина" для RARE-слов, которые не поместились в "основы"-наборы 200–219.

**Что нужно сделать:** для каждой из 9 тем создать набор "Тема: продвинутый" (25 RARE-слов),
распределить слова из set 211 по ним, остаток (если слов больше 25 на тему) — в новый набор или убрать.
Set 211 после этого — настоящая "Архитектура" с 25 новыми словами, или переименовать.

### Sets 200–219 "основы" — недозаполнены
При split-миграции 26→27 RARE-слова из этих наборов ушли в углублённо (240–248),
но замены добавлены не были. Наборы имеют 11–15 слов вместо 25.
**Что нужно сделать:** дописать COMMON/UNCOMMON слова по теме до 25.

---

## Миграция БД (выполнить ПОСЛЕ завершения всех наборов)

```kotlin
val MIGRATION_35_36 = object : Migration(35, 36) {
    override fun migrate(db: SupportSQLiteDatabase) {
        // Полный сброс — WordLoader пересеет при следующем запуске
        db.execSQL("DELETE FROM words")
        db.execSQL("DELETE FROM word_sets")
        db.execSQL("DELETE FROM progress")
        db.execSQL("DELETE FROM collection")
        db.execSQL("DELETE FROM deck_cards")
        db.execSQL("DELETE FROM retained_favorites")
    }
}
```

Добавить в `AppDatabase.kt` + поднять `version = 36` + зарегистрировать миграцию.
**Статус:** ✅ выполнено 2026-04-17

---

## Прогресс

Статусы: ⏳ ожидает · 🔄 в работе · ✅ готово

### WordDataEnglish.kt — сеты 1–87

| Сеты | Описание | Статус |
|------|----------|--------|
| 1–58 | Базовые темы (Животные, Еда, Дом, Транспорт...) — уровни ✓, дубли ✓, производные ✓ | ✅ |
| 59–83 | Продвинутые темы C1/C2 (Философия, Наука, Право...) — уровни ✓, дубли ✓, производные ✓ | ✅ |
| 87 | Логика: базовые понятия — описание B2→B2/C1, LEGENDARY→RARE (strawman, slippery slope, subjectivity) | ✅ |

### WordDataEnglishExpanded.kt — сеты 200–249

| Сеты | Описание | Статус |
|------|----------|--------|
| 200–211 | Тематические основы-наборы (Супермаркет, Инструменты, Праздники...) — 25 слов ✓, производные ✓ | ✅ |
| 212–218 | Тематические наборы (Медицина, Экология, Бизнес...) — EPIC/LEGENDARY → RARE, производные ✓ | ✅ |
| 219 | Химия: основы | ✅ |
| 220–239 | Парные наборы основы/углублённо (Право, Мода, Литература...) — уровни чистые | ✅ |
| 240–247 | "Углублённо"-наборы (RARE/EPIC/LEGENDARY, по 25 слов) | ✅ |
| 248 | Химия: углублённо — exothermic (UNCOMMON) исправлен → electrode (RARE) | ✅ |
| 249 | Компьютерные игры — широкий диапазон без лейбла, оставлен как есть | ✅ |

### WordDataEnglishBatch3–6.kt — сеты 250–261

| Сеты | Описание | Статус |
|------|----------|--------|
| 250–261 | Разные темы B1/B2/C1 — уровни исправлены (88 замен), производные ✓ | ✅ |

### WordDataEnglishBatch7–12.kt — сеты 262–283

| Сеты | Описание | Статус |
|------|----------|--------|
| 262–283 | Начальные наборы A1/A2 (Фрукты, Глаголы, Школа...) — 25 слов ✓, уровни чистые | ✅ |

### Hebrew — сеты 1001–1141

| Сеты | Файл | Статус |
|------|------|--------|
| 1001–1007 | WordDataHebrew.kt, WordDataHebrewEveryday.kt | ✅ |
| 1008–1021 | WordDataHebrewMore.kt, WordDataHebrewAdvanced.kt, WordDataHebrewTech.kt | ✅ |
| 1022–1037 | WordDataHebrewImmigrant*.kt | ✅ |
| 1038–1069 | WordDataHebrewBatch5–15.kt — кросс-дубли ✓ | ✅ |
| 1070–1091 | WordDataHebrewBatch16–23.kt — кросс-дубли ✓ | ✅ |
| 1121–1133 | WordDataHebrewBatch33–37.kt | ✅ |
| 1092–1120, 1134–1141 | WordDataHebrewBatch24–32, 38–39.kt | ✅ (аудит: 0 нарушений, все по 25 слов) |

---

## Порядок работы (каждая сессия)

1. Открыть этот файл — найти первый ⏳
2. Прочитать соответствующий `WordData*.kt`
3. Для каждого набора: выписать все слова → проверить уровень → убрать чужие → добавить нужные до 25
4. Проверить дубли: `grep -rF "original = \"слово\"" data/`
5. Собрать: `JAVA_HOME="C:/Program Files/Android/Android Studio/jbr" ./gradlew assembleDebug`
6. Обновить статус в этом файле → коммит → пуш
7. Следующий набор

**Начинать с:** сеты 1–10 (WordDataEnglish.kt) — базовые, хорошо понятный уровень, задают стандарт для всего остального.
